% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{chains_gibbs_sampling_lbm_cov_poisson}
\alias{chains_gibbs_sampling_lbm_cov_poisson}
\title{Run nchains of the latent phylogenetic Poisson LBM Gibbs sampler}
\usage{
chains_gibbs_sampling_lbm_cov_poisson(nchains, ...)
}
\arguments{
\item{nchains}{the number of chains to run concurrently}

\item{...}{
  Arguments passed on to \code{\link{gibbs_sampling_lbm_cov_poisson}}
  \describe{
    \item{\code{Sigma}}{Numeric covariance matrix for row
latent effects.}
    \item{\code{Y}}{Non-negative integer matrix of observed
counts.}
    \item{\code{init_Z}}{Initial row-membership indicator
matrix (\code{nrow(Y)} x \code{K}).}
    \item{\code{init_W}}{Initial column-membership indicator
matrix (\code{ncol(Y)} x \code{R}).}
    \item{\code{K}}{Number of row groups.}
    \item{\code{R}}{Number of column groups.}
    \item{\code{niter}}{Number of Gibbs iterations.}
    \item{\code{niter_metropolis}}{Number of Metropolis updates
per row for \code{P}.}
    \item{\code{priors_hyper_params}}{List of hyperparameters:
\code{alpha_0}, \code{beta_0},
\code{gammas_0}, \code{a0}, \code{b0}.}
    \item{\code{sigma2_fixed}}{Logical; if \code{TRUE}, keeps
\code{sigma2 = 1}, otherwise samples
\code{sigma2} from its inverse-gamma full conditional.}
    \item{\code{minibatch}}{Logical; if \code{TRUE} the update order
for P will change on each Gibbs iteration}
    \item{\code{tol}}{Numeric; a small number indicating the
tolerance at which one wants to clamp the values,
i.e., if \code{value<tol} it gets set to tol and if
\code{value>1-tol} it gets set to \code{1-tol}. Only used for
probabilities.}
    \item{\code{verbose}}{Logical; if \code{TRUE} the iterations
will print a message each 10 iterations}
    \item{\code{prefix}}{Optional character string; a prefix to
append before logs. Will not print anything if
\code{verbose=FALSE}. Defaults to '' (empty string)}
    \item{\code{known_P}}{NULL (default) or a matrix (\eqn{n_1\times K-1}) indicating the known latent correlated position used to compute probabilities of blocks}
    \item{\code{P_sampler}}{a function indicating the sampler to use for the P
variable. Default to sample_P_metropolis_trick}
    \item{\code{rho}}{a double indicating the variance of the gaussian proposal distribution.}
    \item{\code{known_alpha}}{NULL (default) or a matrix (\eqn{K\times R}) indicating the known \eqn{\alpha} connectivity matrix}
    \item{\code{known_Z}}{NULL (default) or a matrix (\eqn{n_1\times K})
indicating the known columns block memberships}
    \item{\code{known_W}}{NULL (default) or a matrix (\eqn{n_2\times R})
indicating the known columns block memberships}
  }}
}
\value{
A list of length \code{nchains} where each element is the output
of a single call to \code{\link[=gibbs_sampling_lbm_cov_poisson]{gibbs_sampling_lbm_cov_poisson()}}.
}
\description{
Runs several independent chains of \code{\link[=gibbs_sampling_lbm_cov_poisson]{gibbs_sampling_lbm_cov_poisson()}}
concurrently, prefixing each chain logs with its index.
}
