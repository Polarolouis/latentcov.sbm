% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{gibbs_sampling_lbm_cov_poisson}
\alias{gibbs_sampling_lbm_cov_poisson}
\title{Gibbs sampler for latent phylogenetic Poisson LBM}
\usage{
gibbs_sampling_lbm_cov_poisson(
  Sigma,
  Y,
  init_Z,
  init_W,
  K,
  R,
  niter = 50L,
  niter_metropolis = 10L,
  priors_hyper_params = list(alpha_0 = 1, beta_0 = 1, gammas_0 = rep(2, R), a0 = 1, b0 =
    1),
  rho = 1,
  sigma2_fixed = TRUE,
  known_alpha = NULL,
  known_P = NULL,
  known_W = NULL,
  known_Z = NULL,
  P_sampler = sample_P_metropolis_trick,
  minibatch = TRUE,
  tol = TOL,
  verbose = FALSE,
  prefix = ""
)
}
\arguments{
\item{Sigma}{Numeric covariance matrix for row
latent effects.}

\item{Y}{Non-negative integer matrix of observed
counts.}

\item{init_Z}{Initial row-membership indicator
matrix (\code{nrow(Y)} x \code{K}).}

\item{init_W}{Initial column-membership indicator
matrix (\code{ncol(Y)} x \code{R}).}

\item{K}{Number of row groups.}

\item{R}{Number of column groups.}

\item{niter}{Number of Gibbs iterations.}

\item{niter_metropolis}{Number of Metropolis updates
per row for \code{P}.}

\item{priors_hyper_params}{List of hyperparameters:
\code{alpha_0}, \code{beta_0},
\code{gammas_0}, \code{a0}, \code{b0}.}

\item{sigma2_fixed}{Logical; if \code{TRUE}, keeps
\code{sigma2 = 1}, otherwise samples
\code{sigma2} from its inverse-gamma full conditional.}

\item{minibatch}{Logical; if \code{TRUE} the update order
for P will change on each Gibbs iteration}

\item{tol}{Numeric; a small number indicating the
tolerance at which one wants to clamp the values,
i.e., if \code{value<tol} it gets set to tol and if
\code{value>1-tol} it gets set to \code{1-tol}. Only used for
probabilities.}

\item{verbose}{Logical; if \code{TRUE} the iterations
will print a message each 10 iterations}

\item{prefix}{Optional character string; a prefix to
append before logs. Will not print anything if
\code{verbose=FALSE}. Defaults to '' (empty string)}
}
\value{
A list with sampled trajectories:
\code{sigma2_array}, \code{P_array}, \code{W_array}, \code{Z_array}, \code{rho_array}, \code{alpha_array}.
}
\description{
Runs a Gibbs sampler for a Poisson latent block model with latent row effects
\code{P} structured by the covariance matrix \code{Sigma}.
}
