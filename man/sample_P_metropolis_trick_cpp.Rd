% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{sample_P_metropolis_trick_cpp}
\alias{sample_P_metropolis_trick_cpp}
\title{A Metropolis-Hastings sampler for P with the clever proposition (C++)}
\usage{
sample_P_metropolis_trick_cpp(
  P,
  Z,
  Sigma,
  sigma2,
  minibatch = TRUE,
  niter_metropolis = 50L,
  rho = 1
)
}
\arguments{
\item{P}{a matrix of size \eqn{n_1 \times K-1} specifying latent
position of the nodes probabilities of membership in the latent
space}

\item{Z}{a matrix of size \eqn{n_1\times K} with a single 1 per line
indicating the membership of row node \eqn{i}, \eqn{Z_{i,k} = 1} if \eqn{i} is in group \eqn{k} 0 else}

\item{Sigma}{a covariance matrix describing the covariance between row nodes \eqn{(n_1\times n_1)}}

\item{sigma2}{a variance parameter indicating the variance between the K-1 columns of P}

\item{minibatch}{a boolean indicating wether to update the rows in the lexical order or to sample at each iteration. Default to TRUE}

\item{niter_metropolis}{an integer specifying the number of metropolis iterations to perform. Defaults to 50.}

\item{rho}{unused double for compatibility only}
}
\value{
an updated matrix of latent positions, of the same size
as \code{P}
}
\description{
Proposes each row \eqn{P_i} from its full conditional
\eqn{N(\mu_i, \Sigma_i)} (as the R \code{sample_P_metropolis_trick}):
the Gaussian terms cancel in the acceptance ratio, leaving only the
multinomial prior ratio. This is the C++ version of
\code{sample_P_metropolis_trick}.
}
\seealso{
\code{\link[=sample_P_metropolis_classical]{sample_P_metropolis_classical()}},
\code{\link[=sample_P_metropolis_classical_cpp]{sample_P_metropolis_classical_cpp()}},
\code{\link[=sample_P_metropolis_trick]{sample_P_metropolis_trick()}}
}
